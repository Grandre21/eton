GIRO A — ESITO: PASSA
LA COLLEZIONE SI CREA: sì — collezione creata dal modello "Birre" (rinominata "COLLAUDO 4 SET"), Salva ha portato a un redirect automatico su /collections/{id}/edit senza alcun errore mostrato a schermo. Nessun "permission denied for table collections" né altro errore.
VOTO AL BUIO DOPO IL SALVATAGGIO: acceso — verificato due volte: (1) subito dopo il redirect post-salvataggio, checkbox blu/spuntata; (2) dopo un ciclo di navigazione completo e indipendente (Collezioni → apri "COLLAUDO 4 SET" → Modifica collezione), stessa checkbox ancora spuntata. Il valore non si perde: la correzione scartata (ignoreOnInsert) non si sarebbe comportata così.
DOVE COMPARE L'ESITO: il segnale principale è il redirect stesso — l'URL passa a /collections/{id}/edit, il titolo diventa il nome della collezione e compare per la prima volta il pulsante "Elimina": tutto ciò accade nello stesso pannello del form, non altrove. Non ho osservato un toast/banner di conferma separato. Nello stesso riquadro, appena sopra il pulsante "Salva", c'è una riga di stato testuale persistente ("Non c'è niente da salvare: non hai ancora cambiato niente.") che riflette lo stato di salvataggio proprio accanto al pulsante — non lontano da dove si guarda, coerente con la correzione del rilievo 2.
CONSOLE: un'unica eccezione, comparsa due volte identica: "Error: A listener indicated an asynchronous response by returning true, but the message channel closed before a response was received" (una al caricamento iniziale della Home, una dopo una navigazione interna). È il pattern tipico di un'estensione Chrome che intercetta i messaggi (compatibile con il widget di chat iniettato descritto nel brief), non riporta alcun file dell'app né uno stack applicativo. Nessuna riga [Auth]. Nessun altro errore o warning.
RESTA NEL DATABASE: una collezione "COLLAUDO 4 SET" nello spazio Personale (id 21cb3ec5-1286-4026-8d0f-2736d00b863c), creata dal modello Birre, 5 campi (Birrificio, Stile, Gradazione %, Formato, Prezzo €), Voto al buio acceso, 0 elementi al suo interno. Nessun'altra riga toccata o creata.
ALTRO CHE HAI VISTO: tutte le chiamate di rete verso Supabase catturate nella sessione sono terminate con stato 200 (2× GET collections?id=eq..., 2× POST rpc/review_counts); nessuna 4xx/5xx osservata in nessuna fase, in particolare nessun 42501/permission-denied. Il log di rete non ha però mostrato la chiamata di INSERT vera e propria della creazione (solo le riletture successive del record): la registrazione era armata fin dall'apertura della tab, quindi non è chiaro perché non compaia — la considero un limite del tool di cattura, non un'anomalia dell'app, perché il dato risulta comunque scritto e confermato da due riletture indipendenti. Lo spazio Personale era vuoto come atteso prima di iniziare.
